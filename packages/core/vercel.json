{
  "buildCommand": "cd ../.. && npx nx run core:build",
  "ignoreCommand": "cd ../.. && npx nx-ignore core",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
